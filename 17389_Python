N , S = input() , input()

score , bonus = 0,0

for idx , OX in enumerate(S): #iterable한 값을 반환해주는 함수
    if OX == 'O':
        score += idx + 1 + bonus
        bonus += 1
    else:
        bonus = 0

print(score)
